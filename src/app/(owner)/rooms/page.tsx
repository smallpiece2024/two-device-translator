/**
 * オーナー用ルーム一覧ページ（プレースホルダ）。
 *
 * このタスク（bd-xev: middleware＋ルートグループ分離）では、
 * `(owner)/layout.tsx` と `middleware.ts` の認証境界を検証するための
 * 最小限のプレースホルダとして配置する。
 *
 * 本実装（ルーム一覧の取得・表示、作成導線等）は後続タスクで置き換える
 * （`docs/design/app-architecture.md` の `/rooms` 参照）。
 */
export default function RoomsPage() {
  return (
    <main>
      <h1>ルーム一覧（準備中）</h1>
    </main>
  );
}
